---
import { getLangFromUrl, useTranslations } from '../utils/i18n';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<section id="sobre-mi" class="section bg-dark-lighter">
  <div class="container-custom">
    <h2 class="text-4xl md:text-5xl font-bold text-center mb-12">
      {t('about.title')} <span class="gradient-text">{t('about.titleHighlight')}</span>
    </h2>
    
    <div class="grid md:grid-cols-2 gap-12 items-center">
      <div>
        <div class="card">
          <h3 class="text-2xl font-bold mb-4 text-primary">{t('about.greeting')}</h3>
          <p class="text-gray-300 mb-4 leading-relaxed">
            <Fragment set:html={t('about.paragraph1')} />
          </p>
          <p class="text-gray-300 mb-4 leading-relaxed">
            {t('about.paragraph2')}
          </p>
          <p class="text-gray-300 leading-relaxed">
            {t('about.paragraph3')}
          </p>
        </div>
      </div>

      <div class="space-y-6">
        <div class="card">
          <div class="flex items-start gap-4">
            <div class="text-primary text-3xl">💼</div>
            <div>
              <h4 class="text-xl font-semibold mb-2">{t('about.cards.experience.title')}</h4>
              <p class="text-gray-400">
                <Fragment set:html={t('about.cards.experience.description')} />
              </p>
            </div>
          </div>
        </div>

        <div class="card">
          <div class="flex items-start gap-4">
            <div class="text-secondary text-3xl">🎓</div>
            <div>
              <h4 class="text-xl font-semibold mb-2">{t('about.cards.education.title')}</h4>
              <p class="text-gray-400">
                {t('about.cards.education.description')}
              </p>
            </div>
          </div>
        </div>

        <div class="card">
          <div class="flex items-start gap-4">
            <div class="text-primary text-3xl">🚀</div>
            <div>
              <h4 class="text-xl font-semibold mb-2">{t('about.cards.methodologies.title')}</h4>
              <p class="text-gray-400">
                {t('about.cards.methodologies.description')}
              </p>
            </div>
          </div>
        </div>

        <div class="card">
          <div class="flex items-start gap-4">
            <div class="text-secondary text-3xl">📚</div>
            <div>
              <h4 class="text-xl font-semibold mb-2">{t('about.cards.learning.title')}</h4>
              <p class="text-gray-400">
                {t('about.cards.learning.description')}
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

